{% extends "header.html.j2" %}
{% block page_name %}Schüler{% endblock %}
{% block content %}
    <table>
        <thead>
            <tr>
                <th>Schüler ID</th>
                <th>Klassen ID</th>
                <th>Konto ID</th>
                <th>Vorname</th>
                <th>Nachname</th>
                <th>Geburtstag</th>
                <th>Aktionen</th>
            </tr>
        </thead>
        <tbody>
            {% for user_data in users %}
                <tr>
                    <td>{{ user_data["id"] }}</td>
                    <td>{{ user_data["school_class_id"] }}</td>
                    <td>{{ user_data["account"]["id"] }}</td>
                    <td>{{ user_data["account"]["name"] }}</td>
                    <td>{{ user_data["account"]["last_name"] }}</td>
                    <td>{{ user_data["account"]["birthday"] }}</td>
                    <td>
                        <form method="post">
                        <input type="hidden" name="id" value="{{ user_data["id"] }}">
                            <input type="submit" name="modify" value="Ändern">
                            <input type="submit" name="delete" value="Löschen">
                        </form>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <form method="post">
        <input type="submit" name="new" value="+">
    </form>
{% endblock %}